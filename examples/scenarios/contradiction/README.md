# Contradiction Scenario

Two live partner memos cite the same external authority but record opposite structured conclusions. Solomon flags both as stale-pending human review and records the contradiction in the audit chain.

The warehouse baseline returns both memos as ordinary text hits and has no contradiction signal.

Run:

```sh
uv run python examples/scenarios/contradiction/run.py
```
