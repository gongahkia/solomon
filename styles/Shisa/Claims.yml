extends: existence
message: "Avoid absolute claims like '%s' unless the doc has evidence or a scope qualifier."
level: warning
ignorecase: true
tokens:
  - never
  - always
  - guaranteed
  - guarantees
  - impossible
