# Shisa — Active TODO

Read [north-star.md](north-star.md) first. The next additions must strengthen the target contract, not recreate a context manager or prompt.

- [ ] Add a fixture-backed Kubernetes config-view test covering context, namespace, cluster-server, and explicit flags.
- [ ] Define an explicit opt-in provider-identity refresh contract before adding AWS, GCP, or Azure checks.
- [ ] Preserve a direct child command's exit status without expanding the accepted executable boundary.
- [ ] Evaluate a stable `shisa.target/v1` JSON schema only after a real runbook consumer exists.
