[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "stonks-cli-fixture-provider"
version = "1.0.0"
requires-python = ">=3.11"

[project.entry-points."stonks_cli.providers"]
fixture = "fixture_provider:provider"
