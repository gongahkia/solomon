{
  "name": "decorum",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Community-Notes-style context cards for LinkedIn posts.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "npm run check && npm run test:runtime && npm run test:visual",
    "test:runtime": "node scripts/test-runtime.mjs",
    "test:visual": "node scripts/test-visual.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
