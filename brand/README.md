# Brand Tokens

Canonical visual tokens live in `brand/tokens.json`.

Current groups:

- `color`: primary, accent, danger, ink, and surface colors
- `font`: prose and mono font stacks

Use these tokens for docs, landing pages, screenshots, social cards, and future logo work. Additive changes are preferred; renames should include a migration note because downstream sites may read this JSON directly.
