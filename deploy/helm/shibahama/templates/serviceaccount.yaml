{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "shibahama.serviceAccountName" . }}
  labels:
    {{- include "shibahama.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
